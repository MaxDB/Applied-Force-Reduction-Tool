Adding additional settings or modifying default values
In most cases settings should be changed programmatically 

"#" are ignored
"," are delimeters
Format is [option_name,option_value,short description]
Don't use whitespace in names
Place string values in double quotation marks